{
  "name": "<%= requirifyModuleName %>",
  "version": "0.0.0",
  "description": "requirify wrap for <%= moduleName %>",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "<%= moduleName %>": "latest"
  }
}
